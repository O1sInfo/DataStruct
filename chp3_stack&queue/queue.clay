队列queue是一种先进先出的线性表,它只允许在表的一端插入，而在另一端删除。
允许插入的一端叫队尾，允许删除的一端是队头。

队列在程序中的最典型例子是操作系统中的作业排队。

链队列--队列的链式表示和实现

这里，和线性表的单链表一样，为了操作方便，我们也给链队列添加一个头结点，并令头指针指向头结点。
空的链队列的判决条件为头指针和尾指针均指向头结点。


循环队列--队列的顺序表示和实现

为了在c语言中描述方便，在此我们约定：初始化建立队列时，令front=rear=0, 每当插入新的队列尾元素时，尾指针增加1；
每当删除队列头元素时，头元素增1。因此在非空队列中。头元素始终指向队列的头元素，而尾元素始终指向队尾元素的下一个位置。

使用循环队列必须设定一个最大队列元素。
为了避免rear=front是空队列或满队列的二义性，留出一个单元不使用，并在进队前测试 满队列条件。
循环队列的满队列条件：(rear + 1)%MAXQSIZE == front
